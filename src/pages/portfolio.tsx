import React from "react";
import { Layout } from "../components/Layout";

const Portfolio = () => {
  return (
    <Layout>
      <span>Portfolio</span>
    </Layout>
  );
};

export default Portfolio;
